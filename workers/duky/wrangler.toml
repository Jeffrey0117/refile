name = "duky"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# KV namespace for file metadata persistence
[[kv_namespaces]]
binding = "FILE_STORE"
id = "duky-files-dev"
preview_id = "duky-files-preview"

# Secrets (set via: wrangler secret put API_KEY)
# For local dev, create .dev.vars with: API_KEY=dev-test-key
